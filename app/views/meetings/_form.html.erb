<%= form_for(@meeting) do |f| %>
  <% if @meeting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meeting.errors.count, "error") %> prohibited this meeting from being saved:</h2>

      <ul>
      <% @meeting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :meeting_minutes1 %><br />
    <%= f.text_field :meeting_minutes1 %>
  </div>
    <div class="field">
    <%= f.label :meeting_minutes2 %><br />
    <%= f.text_field :meeting_minutes2 %>
  </div>
    <div class="field">
    <%= f.label :meeting_minutes3 %><br />
    <%= f.text_field :meeting_minutes3 %>
  </div>

  <div class="field">
    <% @meeting.attendees.each do |attender| %>
      <tr>
        <td><p><%= radio_button_tag 'attendance_#{attender.id}', 'attended' %> Attended </p></td>
        <td><p><%= radio_button_tag 'attendance_#{attender.id}', 'absent' %> Absent </p></td>
        <td><p><%= radio_button_tag 'attendance_#{attender.id}', 'execused' %> Execused </p></td>
        <td><%= attender.name %></td>
      </tr>
    <% end %>
  </div>

  <div class="field">
    <%= f.label :room %><br />
    <%= f.text_field :room %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
