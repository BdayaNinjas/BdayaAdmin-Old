<% @page_title = "Delete Kid: #{@kid.name}" %>

<%= link_to("<< Back to List", {:action => 'index'}) %>

<div>
  <h2>Delete Kid</h2>
  
  <%= form_for(:kid, :url => {:action => 'destroy', :id => @kid.id}, :method => 'DELETE') do |f| -%>

    <p>Are you sure you want to permanently delete <%= @kid.name %>?</p>
  
    <div>
      <%= submit_tag("Delete Kid") %>	
    </div>
    
  <% end %>
</div>
